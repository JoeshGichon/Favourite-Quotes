import { DownvoteDirective } from './downvote.directive';

describe('DownvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new DownvoteDirective();
    expect(directive).toBeTruthy();
  });
});
